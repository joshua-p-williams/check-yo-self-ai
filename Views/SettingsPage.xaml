<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CheckYoSelfAI.ViewModels"
             x:Class="CheckYoSelfAI.Views.SettingsPage"
             x:DataType="viewModels:SettingsPageViewModel"
             Title="Settings"
             SemanticProperties.Description="Application settings">

    <ScrollView>
        <VerticalStackLayout Padding="20"
                             Spacing="16">
            <Label Text="Settings"
                   Style="{StaticResource Headline}"
                   HorizontalOptions="Start"
                   HorizontalTextAlignment="Start"
                   SemanticProperties.HeadingLevel="Level1" />

            <Border Padding="12">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Azure AI Configuration"
                           FontSize="18"
                           FontAttributes="Bold" />

                    <Label Text="Endpoint URL"
                           FontAttributes="Bold"
                           FontSize="13" />
                    <Border Padding="8,4"
                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}">
                        <Border.Triggers>
                            <DataTrigger TargetType="Border"
                                         Binding="{Binding HasEndpointError}"
                                         Value="True">
                                <Setter Property="Stroke"
                                        Value="{StaticResource Error}" />
                            </DataTrigger>
                        </Border.Triggers>
                        <Entry Text="{Binding Endpoint}"
                               Placeholder="https://your-resource.cognitiveservices.azure.com/"
                               Keyboard="Url"
                               ClearButtonVisibility="WhileEditing"
                               x:Name="EndpointEntry"
                               SemanticProperties.Description="Azure endpoint URL" />
                    </Border>
                    <Label Text="{Binding EndpointError}"
                           FontSize="11"
                           TextColor="{StaticResource Error}"
                           IsVisible="{Binding HasEndpointError}" />

                    <Label Text="API Key"
                           FontAttributes="Bold"
                           FontSize="13" />
                    <Border Padding="8,4"
                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}">
                        <Border.Triggers>
                            <DataTrigger TargetType="Border"
                                         Binding="{Binding HasApiKeyError}"
                                         Value="True">
                                <Setter Property="Stroke"
                                        Value="{StaticResource Error}" />
                            </DataTrigger>
                        </Border.Triggers>
                        <Entry Text="{Binding ApiKey}"
                               Placeholder="Paste Azure API key"
                               IsPassword="True"
                               ClearButtonVisibility="WhileEditing"
                               x:Name="ApiKeyEntry"
                               SemanticProperties.Description="Azure API key" />
                    </Border>
                    <Label Text="{Binding ApiKeyError}"
                           FontSize="11"
                           TextColor="{StaticResource Error}"
                           IsVisible="{Binding HasApiKeyError}" />

                    <Label Text="Region"
                           FontAttributes="Bold"
                           FontSize="13" />
                    <Border Padding="8,4"
                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}">
                        <Border.Triggers>
                            <DataTrigger TargetType="Border"
                                         Binding="{Binding HasRegionError}"
                                         Value="True">
                                <Setter Property="Stroke"
                                        Value="{StaticResource Error}" />
                            </DataTrigger>
                        </Border.Triggers>
                        <Entry Text="{Binding Region}"
                               Placeholder="eastus"
                               ClearButtonVisibility="WhileEditing"
                               x:Name="RegionEntry"
                               SemanticProperties.Description="Azure region" />
                    </Border>
                    <Label Text="{Binding RegionError}"
                           FontSize="11"
                           TextColor="{StaticResource Error}"
                           IsVisible="{Binding HasRegionError}" />

                    <Grid ColumnDefinitions="*,Auto"
                          ColumnSpacing="8">
                        <Button x:Name="TestConnectionButton"
                                Grid.Column="0"
                                Text="Test Connection"
                                Command="{Binding TestConnectionCommand}"
                                IsEnabled="{Binding IsNotTestingConnection}"
                                Clicked="OnActionInvoked"
                                SemanticProperties.Description="Test Azure connection" />
                        <ActivityIndicator Grid.Column="1"
                                           IsRunning="{Binding IsTestingConnection}"
                                           IsVisible="{Binding IsTestingConnection}"
                                           VerticalOptions="Center"
                                           SemanticProperties.Description="Testing connection in progress" />
                    </Grid>

                    <Label Text="{Binding TestConnectionStatusMessage}"
                           FontSize="12"
                           TextColor="{StaticResource Primary}"
                           IsVisible="{Binding HasTestConnectionStatusMessage}"
                           SemanticProperties.Description="Connection status" />
                </VerticalStackLayout>
            </Border>

            <Border Padding="12">
                <VerticalStackLayout Spacing="10">
                    <Label Text="App Preferences"
                           FontSize="18"
                           FontAttributes="Bold" />

                    <Grid ColumnDefinitions="*,Auto"
                          ColumnSpacing="8">
                        <Label Grid.Column="0"
                               Text="Auto-save processing results"
                               VerticalTextAlignment="Center" />
                        <Switch x:Name="AutoSaveSwitch"
                                Grid.Column="1"
                                IsToggled="{Binding AutoSaveResults}"
                                Toggled="OnPreferenceToggled"
                                SemanticProperties.Description="Auto-save processing results" />
                    </Grid>

                    <Grid ColumnDefinitions="*,Auto"
                          ColumnSpacing="8">
                        <Label Grid.Column="0"
                               Text="Show confidence scores by default"
                               VerticalTextAlignment="Center" />
                        <Switch x:Name="ShowConfidenceSwitch"
                                Grid.Column="1"
                                IsToggled="{Binding ShowConfidenceScores}"
                                Toggled="OnPreferenceToggled"
                                SemanticProperties.Description="Show confidence scores by default" />
                    </Grid>

                    <Label Text="Additional preferences will be added in future phases."
                           FontSize="12"
                           TextColor="{StaticResource Gray600}" />
                </VerticalStackLayout>
            </Border>

            <Button x:Name="SaveSettingsButton"
                    Text="Save Settings"
                    Command="{Binding SaveSettingsCommand}"
                    IsEnabled="{Binding IsNotSaving}"
                    Clicked="OnActionInvoked"
                    SemanticProperties.Description="Save settings" />

            <ActivityIndicator IsRunning="{Binding IsSaving}"
                               IsVisible="{Binding IsSaving}"
                               HorizontalOptions="Center"
                               SemanticProperties.Description="Saving settings" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
